services:

  backend:
    build: ./backend
    container_name: cozy-loops-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: cozy-loops-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend